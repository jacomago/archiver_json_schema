# archiver-schema

Repository to store a json schema for output from an "Archiver". "Archiver" being a software that stores data from a control system. The output is the data returned from the "Archiver" when fetching stored data.
